# Generated from scripts/catalog_data.py. Adjust the shared metadata instead of editing this file by hand.
chaos-mesh:
  controllerManager:
    replicaCount: 1
  dashboard:
    service:
      type: ClusterIP
    ingress:
      enabled: false
      ingressClassName: taikun
      hosts:
      - name: chaos-dashboard.local
        tls: true
        tlsSecret: chaos-mesh-tls
      annotations:
        cert-manager.io/cluster-issuer: ccf-default
        external-dns.alpha.kubernetes.io/ttl: '300'
    persistentVolume:
      size: 5Gi
