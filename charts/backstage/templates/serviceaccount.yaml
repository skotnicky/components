{{- if .Values.backstage.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "ccf.backstage.serviceAccountName" . }}
  namespace: {{ .Release.Namespace | quote }}
  labels:
    {{- include "ccf.backstage.labels" . | nindent 4 }}
{{- end }}
