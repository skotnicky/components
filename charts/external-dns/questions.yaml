# Generated from scripts/catalog_data.py.
# This file follows the common Rancher-style questions.yaml shape so catalog UIs can present CCF-friendly prompts.
questions:
- variable: external-dns.provider.name
  label: DNS provider
  type: string
  default: aws
  description: Provider name accepted by the upstream external-dns chart.
  group: Provider
  required: true
- variable: external-dns.txtOwnerId
  label: TXT owner ID
  type: string
  default: ccf-external-dns
  description: Identifier used for TXT registry ownership records.
  group: Provider
  required: true
- variable: external-dns.sources
  label: Watched sources
  type: listofstrings
  default:
  - service
  - ingress
  description: Resource sources external-dns should watch.
  group: Application
  required: false
- variable: external-dns.domainFilters
  label: Domain filters
  type: listofstrings
  default: []
  description: Optional list of DNS zones to scope the controller to.
  group: Provider
  required: false
- variable: external-dns.policy
  label: Update policy
  type: enum
  default: upsert-only
  description: How external-dns should apply record changes.
  group: Provider
  required: false
  options:
  - upsert-only
  - sync
  - create-only
